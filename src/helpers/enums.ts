export enum BoostStep {
  Permit = 'Permit',
  Boost = 'Boost',
}

export enum StakeStep {
  Approve = 'Approve',
  Stake = 'Stake',
}

export enum UnstakeStep {
  Approve = 'Approve',
  Queue = 'Queue',
  Swap = 'Swap',
}
