export enum BoostStep {
  Permit = 'Permit',
  Boost = 'Boost',
}

export enum StakeStep {
  SwapApprove = 'SwapApprove',
  Approve = 'Approve',
  Stake = 'Stake',
  Swap = 'Swap',
}

export enum UnstakeStep {
  Approve = 'Approve',
  Queue = 'Queue',
  Swap = 'Swap',
}
